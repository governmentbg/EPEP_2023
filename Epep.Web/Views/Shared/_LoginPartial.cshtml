@using Epep.Core.Contracts
@inject IUserContext userContext;
@if (userContext.UserId > 0)
{
    <li class="nav-item">
        <a class="nav-link text-dark" asp-controller="Account" asp-action="LogOff">@userContext.FullName, излез!</a>
    </li>
}